# name: learning jmeter integration in ci/cd
# on:
#   push:
#     branches: [main]

# jobs:
#   learning:
#     runs-on: ubuntup-latest
#     env:
#       JMETER_VERSION: 5.6.3

#     steps: 
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup java
#         uses: actions/setup-java@v4

#       - name: Install jmeter
#         runs: |
#           wget -q https://downloads.apache.org/jmeter/binaries/apache-jmeter-${JMETER_VERSION}.tgz
#           tar -xzf apache-jmeter-${JMETER_VERSION}.tgz
#           echo "JMETER_HOME=${pwd}/apache-jmeter-${JMETER_VERSION}" >> $GITHUB_ENV
#           echo "$(pwd)/apache-jmeter-${JMETER_VERSION}/bin" >> $GITHUB_PATH

#       - name: create results files
#         runs: if [! -d results]: then 
#           mkdir results
#           fi

#       - name: verify test file exists
#         runs: if [! -f test2.jmx]; then
#           echo "Error: test2.jmx file doesnot exists"
#           exit 1
#           fi


#       - name: runs jmeter files 
#         runs: |
#           ${JMETER_HOME}/bin/jmeter -n \
#           -t test2.jmx \
#           -l results/test2.jtl 
   
#       - name: create html report
#         runs: |
#           ${JMETER_HOME}/bin/jmeter -g results/test2.jtl -o results/reports


#       - name: upload jmeter report
#         uses: actions/setup-aritifact@v4
#         if: always()
#         with:
#           name: jmeter-report
#           path: results/reports
#           retention-days: 7 
